# 108,272步低学习率恢复记录

## 当前运行

更新：该分支已按用户授权在123,740步完整保存并暂停，随后完成独立落点稳定性试验。
详情见`EXPERT_POSITION_STABILITY_EXPERIMENT_20260830.zh-CN.md`；尚未自动将试验方案切换为正式训练。

- 用户选择此前讨论的108,272步存档（用户消息写作108271，已明确说明实际步数，未改写存档计数）。
- Run：`expert-v1.1-r108272-lr1e-4-20260830`。
- 云端根目录：`/root/autodl-tmp/cr-expert-v1`。
- Run目录：`formal-runs/expert-v1.1-r108272-lr1e-4-20260830`。
- SSH：`root@connect.weste.seetacloud.com:17461`，使用已有本地SSH密钥。
- 活动指针：云端根目录的`active-training-run.json`；进程以Run的`launch.json`为准。
- 2026-08-30T18:23:09Z已真实恢复，随后进度超过108,272步。
- 用户要求在当前对话观察，不创建或启用定时任务。

## 精确变更

- 从原运行`expert-v1.1-cloud-177m-b32`的108,272步恢复。
- 唯一训练超参数变化：原档学习率`3e-4 → 1e-4`。
- 与上一回退实验相同的低学习率，但下调起点提前15,468步。
- 模型权重、AdamW动量/二阶矩/step、所有RNG、epoch数据排列和位置经逐项核对保留。
- 模型177,092,661参数、Batch 32、BF16、序列128、burn-in 32、梯度裁剪1.0、8 workers、prefetch 4等不变。
- 沿用原始split命名：训练使用`validation`，验证使用`train`，测试使用`test`；本轮没有重新划分数据。
- Epoch 2、batch 30,935/77,337；正常1%保存策略与每epoch保存策略不变。

## 备份

原始108,272步固定副本：

`frozen/expert-v1.1-step108272/checkpoint-108272.pt`

- 只读，包含完整Optimizer/RNG，不参与三份滚动轮转。
- SHA-256：`efaf4b7336b59f28a866fe9de5eb4d5befbee3eb3bfe56131aaa09712ce41846`。
- `receipt.json`和原始运行清单在同目录。

上一低学习率分支已停止，最后进度记录139,637步；完整保存的是139,207步，不能将二者混淆。

`frozen/expert-v1.1-r123740-lr1e-4-20260830-step139207/`

- `checkpoint-139207.pt`：2,125,199,333字节；SHA-256 `b49c7c9ead082dd23ce969ad18777343f83b48914f11b76d20d0d24684cb93ba`。
- `weights-139207-fp16.pt`：354,201,290字节；SHA-256 `4c3020baab664c1ca7dd60ad264d831e726347b25d138ce24ad0751879076c99`。
- 模型/优化器张量与FP16导出有限数检查通过，保存源文件与副本哈希一致。
- 回执、停止记录、运行清单和当时日志同目录；本次139,207步产物仅存云端，未下载本地。

## 启动与监控注意事项

- 续训须复用已认证的数据完整性回执：`CR_EXPERT_TRUST_EXISTING_INTEGRITY=1`。
- 该设置只避免重复扫描同一已校验数据；仍核对manifest摘要、文件覆盖和续训契约，不改变训练计算。
- 本次初次启动遗漏该设置，在任何训练更新之前停止了重复校验并修正；没有丢弃优化器更新。
- 修正后的进程：trainer 22782、cache 22783、TensorBoard mirror 22784；以后不要盲用这些PID，读取`launch.json`。
- 缓存请求上限68GiB、安全余量12GiB；实际驻留由安全门决定，不将请求上限当成已使用量。
- TensorBoard：`http://127.0.0.1:16007/`，日志目录`/root/tf-logs/expert-v1.1-r108272-lr1e-4-20260830`。
- 页面仅筛选新Run，展示`train/loss`、位置Loss窗口均值、裁剪前梯度窗口均值和实际学习率，smoothing=0。
- `train/loss`是真实窗口算术均值；恢复后的首个窗口65批，其后通常100批。旧`loss_live`仅是发布时最后一批，比较时不得混用口径。
- 上一分支先有孤立尖峰，后出现尖峰增密与梯度持续上升；不以一次新低或一个正常窗口判定问题已解决。
