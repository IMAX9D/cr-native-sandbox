# CR pooled-entity LSTM BC baseline

See [中文说明](README.zh-CN.md) for architecture, dataset limitations, standalone training/benchmark commands and validation.
Run from the repository root: `python -m hokoff_model.smoke`. Existing `policy_v1` dependencies and prepared cache are reused; no HoK runtime is required.
